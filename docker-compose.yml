services:
  web:
    build: ./src
    ports:
      - "8080:8080"
    volumes:
      - .:/app
    working_dir: /app/src
    command: >
      sh -c "composer install --no-interaction
      && php -S 0.0.0.0:8080 -t /app /app/src/router.php"
